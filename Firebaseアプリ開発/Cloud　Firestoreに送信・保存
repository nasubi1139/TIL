・const submitData = async (e) => {
  e.preventDefault();

  ページ遷移とかのイベントキャンセル
  いつも使ってたやつ
============
・const formData = new FormData(e.target);
FormDataオブジェクトのインスタンス化
FormData => 標準組み込みオブジェクト
============
・try { // 非同期のtry/catch構文 tryは同期成功時の処理
    const docRef = await addDoc(collection(db, "reports"), {
      date: new Date(),
      name: formData.get("name"), // インスタンスしたFormDataのgetメソッド
      work: formData.get("work"), //　引数にkey名(inputタグ等のname属性)を指定し、値を取得
      comment: formData.get("comment"),
    });
    console.log("Document written with ID: ", docRef.id);
  } catch (e) {
    console.error("Error adding document: ", e);
  }

